Bewerk een bestaande ervaringsdeskundige
---
tags: 
- Ervaringsdeskundigen
consumes:
  application/json
produces:
  application/json
parameters:
- name: id
  in: path
  type: integer
  required: true
- in: body
  required: true
  schema:
    $ref: './apidefinitions/#/Ervaringsdeskundige'
responses:
  200:
    description: Een ervaringsdeskundige is succesvol bewerkt
  403:
    description: De API sleutel ontbrak uit het verzoek of had niet voldoende rechten om deze actie uit te voeren
    schema:
      $ref: './apidefinitions/#/Error'
  415:
    description: Het verzoek had missende of invalide data
    schema:
      $ref: './apidefinitions/#/Error'