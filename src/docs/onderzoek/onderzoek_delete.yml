Verwijder een bestaand onderzoek
---
tags: 
- Onderzoeken
produces:
  application/json
parameters:
- name: id
  in: path
  type: integer
  required: true
responses:
  200:
    description: Een onderzoek is succesvol verwijdert
  403:
    description: De API sleutel ontbrak uit het verzoek of had niet voldoende rechten om deze actie uit te voeren
    schema:
      $ref: './apidefinitions/#/Error'
  415:
    description: Het verzoek had missende of invalide data
    schema:
      $ref: './apidefinitions/#/Error'