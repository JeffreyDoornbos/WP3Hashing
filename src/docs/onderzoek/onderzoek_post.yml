Maak een nieuw onderzoek aan
---
tags: 
- Onderzoeken
consumes:
  application/json
produces:
  application/json
parameters:
- in: body
  required: true
  schema:
    $ref: './apidefinitions/#/Onderzoek'
responses:
  200:
    description: Een onderzoek is succesvol aangemaakt
    schema:
      type: int
      description: 'Het id van het aangemaakte onderzoek'
  403:
    description: De API sleutel ontbrak uit het verzoek of had niet voldoende rechten om deze actie uit te voeren
    schema:
      $ref: './apidefinitions/#/Error'
  415:
    description: Het verzoek had missende of invalide data
    schema:
      $ref: './apidefinitions/#/Error'