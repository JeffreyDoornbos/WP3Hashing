Haal een lijst met meerdere organisaties op
---
tags:
- Organisaties
produces:
  application/json
parameters:
- name: limit
  in: query
  schema:
    type: integer
    minimum: 1
    maximum: 50
  required: false
- name: offset
  in: query
  schema:
    type: integer
    minimum: 0
  required: false
responses:
  200:
    description: Een lijst met organisaties
    schema:
      type: array
      items:
        $ref: './apidefinitions/#/Organisatie'