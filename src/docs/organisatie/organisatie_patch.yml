Bewerk een bestaande organisatie
---
tags: 
- Organisaties
consumes:
  application/json
produces:
  application/json
parameters:
- name: id
  in: path
  type: integer
  required: true
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: './apidefinitions/#/Organisatie'
responses:
  200:
    description: Een organisatie is succesvol bewerkt
  415:
    description: De body was niet in het juiste formaat of miste vereiste data, of het id miste of bestond niet